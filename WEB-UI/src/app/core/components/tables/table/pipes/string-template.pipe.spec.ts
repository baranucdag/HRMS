import { StringTemplatePipe } from './string-template.pipe';

describe('StringTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new StringTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
